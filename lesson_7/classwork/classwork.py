import sys


# arg2 = sys.argv[1]
# arg3 = sys.argv[2]

# print(type(arg2), type(arg3))


# Task 1
# def check_guest(is_vip: bool, is_registered: bool, is_invited: bool) -> bool:
#     """
#     Current function validates if user can be accessed to the event.
#     Input should be specified with 1 or 0 for each param.
#     :param is_vip: Contains value if user is VIP
#     :param is_registered: Contains value if user is registered
#     :param is_invited: Contains value if user is invited
#     :return: If user is allowed or not.
#     """
#     if is_vip or (is_registered and is_invited):
#         return True
#     else:
#         return False
#
#
# print(check_guest(bool(int(sys.argv[1])), bool(int(sys.argv[2])), bool(int(sys.argv[3]))))


# sum_of_nums = 0
# for num in sys.argv[1]:
#     sum_of_nums += int(num)
#
# print(sum_of_nums)

print(sys.argv[1])
